import React, { Fragment } from "react";
import { StyleHeader } from "./styles";

const Header = () => (
  <Fragment>
    <StyleHeader>
      <h1>Agenda</h1>
    </StyleHeader>
  </Fragment>
);

export default Header;
